/** CSS class for inline file reference spans inside RichInputArea (contentEditable). */
export const FILE_REF_CLASS = 'dc-file-ref'
/** CSS class for inline command reference spans inside RichInputArea (contentEditable). */
export const COMMAND_REF_CLASS = 'dc-command-ref'
