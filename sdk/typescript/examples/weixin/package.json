{
  "name": "dotcraft-weixin-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DotCraft external channel adapter for WeChat (iLink) using dotcraft-wire",
  "scripts": {
    "start": "node dist/index.js adapter_config.json",
    "build": "tsc",
    "dev": "tsc && node dist/index.js adapter_config.json"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotcraft-wire": "file:../..",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/qrcode-terminal": "^0.12.2",
    "typescript": "^5.7.0"
  }
}
