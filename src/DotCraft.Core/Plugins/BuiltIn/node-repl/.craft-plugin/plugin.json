{
  "schemaVersion": 1,
  "id": "node-repl",
  "version": "1.0.0",
  "displayName": "Node REPL",
  "description": "Desktop-hosted JavaScript evaluation and browser-use runtime for the current thread.",
  "capabilities": ["browser-use", "javascript", "desktop"],
  "functions": [
    {
      "namespace": "node_repl",
      "name": "NodeReplJs",
      "description": "Evaluate JavaScript in the Desktop persistent Node REPL for the current thread. The runtime supports top-level state, agent.browser, display(), and screenshot image output.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "code": { "type": "string" },
          "timeoutSeconds": { "type": "integer" }
        },
        "required": ["code"]
      },
      "backend": {
        "kind": "builtin",
        "providerId": "node-repl",
        "functionName": "NodeReplJs"
      }
    }
  ]
}
